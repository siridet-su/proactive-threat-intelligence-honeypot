# Untracked-files resolution — 2026-07-27

The inventory began with 36 files in four directories, 389,610 apparent bytes
in total. Every file was inspected by type, size, content, committed
references, authoritative replacement, publication suitability, and security
boundary.

| Original path | Size | Classification | Unique value | Authoritative replacement | Final action |
|---|---:|---|---|---|---|
| `evaluation/next_tactic_benchmark_evidence/data_preparation_training_audit/` | 139,292 B | `CONSOLIDATE_THEN_COMMIT` | Narrow scientific claim boundary, reconciled counts, diversity and unavailable-provenance findings | Final-design data/label assessment and committed benchmark evidence | Conclusions and source hashes consolidated into `HISTORICAL_NEXT_TACTIC_AUDITS_20260727.md`; 12 working files deleted |
| `evaluation/next_tactic_benchmark_evidence/execution_persistence_investigation/` | 185,462 B | `CONSOLIDATE_THEN_COMMIT` | Exact explanation of the 819 Execution-to-Persistence errors and integrity checks | Immutable single-checkpoint evaluation and confusion evidence | Conclusions and source hashes consolidated; seven working files deleted |
| `evaluation/next_tactic_benchmark_evidence/gru_transformer_runtime_comparison/` | 22,980 B | `CONSOLIDATE_THEN_COMMIT` | Defensible `NOT_DETERMINABLE` paired-runtime conclusion | Accepted predictive evidence; no valid runtime replacement exists | Blocker and source hashes consolidated; nine empty/intermediate/runtime-host files deleted |
| `local_cache_cleanup_review/` | 41,876 B | `MOVE_OUTSIDE_REPOSITORY` | Manually reviewable, dry-run cache maintenance scripts and point-in-time manifests | None; machine-specific operational material is inappropriate for Git | Moved unchanged to `/home/rubchek/Desktop/honeypot-local-maintenance/local_cache_cleanup_review/` |

## File-level classification

- The three `*.md` reports contained the useful conclusions.
- `audit_summary.json`, `reproducibility_manifest.json`, `summary.json`, and
  the blocked runtime manifest supplied provenance hashes, but their essential
  claims are consolidated above.
- CSV tables, representative case references, preprocessing snapshots,
  environment details, empty timing tables, duplicate metric exports, and
  intermediate manifests were `DELETE_SAFE` after consolidation.
- The eight cache-review shell/TSV/Markdown files were all
  `MOVE_OUTSIDE_REPOSITORY`; they contain host-specific absolute paths and
  maintenance commands. No deletion receipt existed, and the audited cache
  candidates were still present, so the package remains useful locally.
- No item was `AMBIGUOUS_DO_NOT_CHANGE`.

## Security review

The experiment bundles contained privacy-minimized tactic sequences, aggregate
counts, hashes, and pseudonymous case references; they contained no raw
commands, credentials, keys, tokens, email addresses, or source databases.
One summary embedded a local absolute repository path and was not committed.

The cache package identifies local cache, backup, database, HMAC-key-file, and
environment paths but does not contain key values or credentials. Moving it
outside the repository prevents publication of machine-specific inventory.

Tracked retained content was scanned for private-key markers, credentialed
database URIs, and common token assignments. No secret was found.

## Lasting references

Earlier cleanup/status documents that described the bundles as protected
untracked material now point to the consolidated audit. Historical statements
remain accurate as records of their earlier state, but no current handoff
requires an untracked directory.
